# 此文件已禁用（原 Gitee 同步配置含中文键名导致 GitHub Actions 解析报错）
# 如需同步到 Gitee，请参考官方文档重新配置
name: Disabled
on: workflow_dispatch
jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - run: echo "disabled"
